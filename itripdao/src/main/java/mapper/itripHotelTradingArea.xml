<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "./mybatis-3-mapper.dtd">
<mapper namespace="ItripHotelTradingArea">
    <!-- 用户分页查询条件 -->
    <sql id="whereClauseGetItripHotelTradingAreaPage">
        <where>
            <if test="search != null and search  != ''">
            </if>
            <if test="search_1 != null and search_1  != ''">

            </if>
            <if test="search_2 != null and search_2  != ''">

            </if>
        </where>
    </sql>

    <!-- 获取用户列表 -->
    <select id="getItripHotelTradingAreaCount" resultType="java.lang.Integer">
        select count(*) from itrip_hotel_trading_area as table1
        <include refid="whereClauseGetItripHotelTradingAreaPage"></include>
    </select>

    <!-- 获取用户列表 分页-->
    <select id="getItripHotelTradingAreaPage" resultType="cn.itrip.beans.pojo.ItripHotelTradingArea" parameterType="map">
        SELECT
                    id as id,
                    hotelId as hotelId,
                    areaId as areaId,
                    creationDate as creationDate,
                    createdBy as createdBy,
                    modifyDate as modifyDate,
                    modifiedBy as modifiedBy
        FROM
        itrip_hotel_trading_area table1
        <include refid="whereClauseGetItripHotelTradingAreaPage"></include>
        order by table1.creationDate desc
    </select>
</mapper>