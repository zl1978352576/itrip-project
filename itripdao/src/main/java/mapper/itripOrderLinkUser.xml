<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "./mybatis-3-mapper.dtd">
<mapper namespace="ItripOrderLinkUser">
    <!-- 用户分页查询条件 -->
    <sql id="whereClauseGetItripOrderLinkUserPage">
        <where>
            <if test="search != null and search  != ''">
            </if>
            <if test="search_1 != null and search_1  != ''">

            </if>
            <if test="search_2 != null and search_2  != ''">

            </if>
        </where>
    </sql>

    <!-- 获取用户列表 -->
    <select id="getItripOrderLinkUserCount" resultType="java.lang.Integer">
        select count(*) from itrip_order_link_user as table1
        <include refid="whereClauseGetItripOrderLinkUserPage"></include>
    </select>

    <!-- 获取用户列表 分页-->
    <select id="getItripOrderLinkUserPage" resultType="cn.itrip.beans.pojo.ItripOrderLinkUser" parameterType="map">
        SELECT
                    id as id,
                    orderId as orderId,
                    linkUserId as linkUserId
        FROM
        itrip_order_link_user table1
        <include refid="whereClauseGetItripOrderLinkUserPage"></include>
        order by table1.creationDate desc
    </select>
</mapper>