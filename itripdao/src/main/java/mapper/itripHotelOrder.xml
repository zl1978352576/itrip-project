<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "./mybatis-3-mapper.dtd">
<mapper namespace="ItripHotelOrder">
    <!-- 用户分页查询条件 -->
    <sql id="whereClauseGetItripHotelOrderPage">
        <where>
            <if test="search != null and search  != ''">
            </if>
            <if test="search_1 != null and search_1  != ''">

            </if>
            <if test="search_2 != null and search_2  != ''">

            </if>
        </where>
    </sql>

    <!-- 获取用户列表 -->
    <select id="getItripHotelOrderCount" resultType="java.lang.Integer">
        select count(*) from itrip_hotel_order as table1
        <include refid="whereClauseGetItripHotelOrderPage"></include>
    </select>

    <!-- 获取用户列表 分页-->
    <select id="getItripHotelOrderPage" resultType="cn.itrip.beans.pojo.ItripHotelOrder" parameterType="map">
        SELECT
                    id as id,
                    userId as userId,
                    orderNo as orderNo,
                    hotelId as hotelId,
                    hotelName as hotelName,
                    roomId as roomId,
                    count as count,
                    bookingDays as bookingDays,
                    checkInDate as checkInDate,
                    checkOutDate as checkOutDate,
                    orderStatus as orderStatus,
                    payAmount as payAmount,
                    payType as payType,
                    noticePhone as noticePhone,
                    noticeEmail as noticeEmail,
                    creationDate as creationDate,
                    createdBy as createdBy,
                    modifyDate as modifyDate,
                    modifiedBy as modifiedBy
        FROM
        itrip_hotel_order table1
        <include refid="whereClauseGetItripHotelOrderPage"></include>
        order by table1.creationDate desc
    </select>
</mapper>