<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "./mybatis-3-mapper.dtd">
<mapper namespace="ItripHotel">
    <!-- 用户分页查询条件 -->
    <sql id="whereClauseGetItripHotelPage">
        <where>
            <if test="cityId != null and cityId  != ''">
                table1.cityId=#{cityId}
            </if>
            <if test="search_1 != null and search_1  != ''">

            </if>
            <if test="search_2 != null and search_2  != ''">

            </if>
        </where>
    </sql>

    <!-- 获取用户列表 -->
    <select id="getItripHotelCount" resultType="java.lang.Integer">
        select count(*) from itrip_hotel as table1
        <include refid="whereClauseGetItripHotelPage"></include>
    </select>

    <!-- 获取用户列表 分页-->
    <select id="getItripHotelPage" resultType="cn.itrip.beans.pojo.ItripHotel" parameterType="map">
        SELECT
                    id as id,
                    hotelName as hotelName,
                    countryId as countryId,
                    countryName as countryName,
                    provinceId as provinceId,
                    provinceName as provinceName,
                    cityId as cityId,
                    cityName as cityName,
                    address as address,
                    details as details,
                    facilities as facilities,
                    hotelPolicy as hotelPolicy,
                    hotelType as hotelType,
                    hotelLevel as hotelLevel,
                    isGroupPurchase as isGroupPurchase,
                    minPrice as minPrice,
                    maxPrice as maxPrice,
                    hotelStore as hotelStore,
                    creationDate as creationDate,
                    createdBy as createdBy,
                    modifyDate as modifyDate,
                    modifiedBy as modifiedBy
        FROM
        itrip_hotel table1
        <include refid="whereClauseGetItripHotelPage"></include>
        order by table1.creationDate desc
    </select>
</mapper>