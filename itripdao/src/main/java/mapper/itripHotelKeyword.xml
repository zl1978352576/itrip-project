<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "./mybatis-3-mapper.dtd">
<mapper namespace="ItripHotelKeyword">
    <!-- 用户分页查询条件 -->
    <sql id="whereClauseGetItripHotelKeywordPage">
        <where>
            <if test="search != null and search  != ''">
            </if>
            <if test="search_1 != null and search_1  != ''">

            </if>
            <if test="search_2 != null and search_2  != ''">

            </if>
        </where>
    </sql>

    <!-- 获取用户列表 -->
    <select id="getItripHotelKeywordCount" resultType="java.lang.Integer">
        select count(*) from itrip_hotel_keyword as table1
        <include refid="whereClauseGetItripHotelKeywordPage"></include>
    </select>

    <!-- 获取用户列表 分页-->
    <select id="getItripHotelKeywordPage" resultType="cn.itrip.beans.pojo.ItripHotelKeyword" parameterType="map">
        SELECT
                    id as id,
                    hotelId as hotelId,
                    keywordId as keywordId
        FROM
        itrip_hotel_keyword table1
        <include refid="whereClauseGetItripHotelKeywordPage"></include>
        order by table1.creationDate desc
    </select>
</mapper>