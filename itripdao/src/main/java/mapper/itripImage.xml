<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "./mybatis-3-mapper.dtd">
<mapper namespace="ItripImage">
    <!-- 用户分页查询条件 -->
    <sql id="whereClauseGetItripImagePage">
        <where>
            <if test="search != null and search  != ''">
            </if>
            <if test="search_1 != null and search_1  != ''">

            </if>
            <if test="search_2 != null and search_2  != ''">

            </if>
        </where>
    </sql>

    <!-- 获取用户列表 -->
    <select id="getItripImageCount" resultType="java.lang.Integer">
        select count(*) from itrip_image as table1
        <include refid="whereClauseGetItripImagePage"></include>
    </select>

    <!-- 获取用户列表 分页-->
    <select id="getItripImagePage" resultType="cn.itrip.beans.pojo.ItripImage" parameterType="map">
        SELECT
                    id as id,
                    type as type,
                    targetId as targetId,
                    imgUrl as imgUrl,
                    creationDate as creationDate,
                    createdBy as createdBy,
                    modifyDate as modifyDate,
                    modifiedBy as modifiedBy
        FROM
        itrip_image table1
        <include refid="whereClauseGetItripImagePage"></include>
        order by table1.creationDate desc
    </select>
</mapper>