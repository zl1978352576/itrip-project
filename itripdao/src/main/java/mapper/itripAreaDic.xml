<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "./mybatis-3-mapper.dtd">
<mapper namespace="ItripAreaDic">
    <!-- 用户分页查询条件 -->
    <sql id="whereClauseGetItripAreaDicPage">
        <where>
            <if test="search != null and search  != ''">
            </if>
            <if test="search_1 != null and search_1  != ''">

            </if>
            <if test="search_2 != null and search_2  != ''">

            </if>
        </where>
    </sql>

    <!-- 获取用户列表 -->
    <select id="getItripAreaDicCount" resultType="java.lang.Integer">
        select count(*) from itrip_area_dic as table1
        <include refid="whereClauseGetItripAreaDicPage"></include>
    </select>

    <!-- 获取用户列表 分页-->
    <select id="getItripAreaDicPage" resultType="cn.itrip.beans.pojo.ItripAreaDic" parameterType="map">
        SELECT
                    id as id,
                    name as name,
                    areaNo as areaNo,
                    parent as parent,
                    isActivated as isActivated,
                    isTradingArea as isTradingArea,
                    isHot as isHot,
                    level as level,
                    creationDate as creationDate,
                    createdBy as createdBy,
                    modifyDate as modifyDate,
                    modifiedBy as modifiedBy
        FROM
        itrip_area_dic table1
        <include refid="whereClauseGetItripAreaDicPage"></include>
        order by table1.creationDate desc
    </select>
</mapper>