<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "./mybatis-3-mapper.dtd">
<mapper namespace="ItripUserLinkUser">
    <!-- 用户分页查询条件 -->
    <sql id="whereClauseGetItripUserLinkUserPage">
        <where>
            <if test="search != null and search  != ''">
            </if>
            <if test="search_1 != null and search_1  != ''">

            </if>
            <if test="search_2 != null and search_2  != ''">

            </if>
        </where>
    </sql>

    <!-- 获取用户列表 -->
    <select id="getItripUserLinkUserCount" resultType="java.lang.Integer">
        select count(*) from itrip_user_link_User as table1
        <include refid="whereClauseGetItripUserLinkUserPage"></include>
    </select>

    <!-- 获取用户列表 分页-->
    <select id="getItripUserLinkUserPage" resultType="cn.itrip.beans.pojo.ItripUserLinkUser" parameterType="map">
        SELECT
                    id as id,
                    linkUserName as linkUserName,
                    linkIdCard as linkIdCard,
                    linkPhone as linkPhone,
                    userId as userId,
                    creationDate as creationDate,
                    createdBy as createdBy,
                    modifyDate as modifyDate,
                    modifiedBy as modifiedBy
        FROM
        itrip_user_link_User table1
        <include refid="whereClauseGetItripUserLinkUserPage"></include>
        order by table1.creationDate desc
    </select>
</mapper>